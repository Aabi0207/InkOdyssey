Pages folder: contains simple page wrappers for components:

- RegisterPage.jsx -> renders components/Register/Register
- LoginPage.jsx -> renders components/Login/Login

To enable routing in `App.jsx` you need `react-router-dom` installed:

npm install react-router-dom@6

Then start the dev server:

npm run dev
